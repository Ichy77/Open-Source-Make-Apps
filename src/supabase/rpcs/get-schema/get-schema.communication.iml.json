{
	"url": "https://{{connection.projectId}}.supabase.co/rest/v1/",
	"method": "GET",
	"qs": {},
	"body": {},
	"headers": {},
	"response": {
		"iterate": "{{body.users}}",
		"output": {
			"label": "{{item.name}}",
			"value": "{{item.id}}"
		}
	}
}