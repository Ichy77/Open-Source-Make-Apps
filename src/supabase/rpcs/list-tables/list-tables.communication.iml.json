{
	"url": "https://{{connection.projectId}}.supabase.co/rest/v1/",
	"method": "GET",
	"qs": {},
	"body": {},
	"headers": {},
	"response": {
		"iterate": "{{getTableNames(body.paths)}}",
		"output": {
			"label": "{{item}}",
			"value": "{{item}}"
		}
	}
}