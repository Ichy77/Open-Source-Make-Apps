{
    // Request to API endpoint with parameter "id" defined in Mappable parameters.
    "url": "/v2alpha/generation/image-to-video/result/{{parameters.id}}", // Relative to base URL
    "method": "GET",
    "headers": { }, // Additional HTTP headers
    "qs": { }, // Query string

    // Response handling
    "response": {
        "output": {
            "data": "{{body}}"
        }
    }
}