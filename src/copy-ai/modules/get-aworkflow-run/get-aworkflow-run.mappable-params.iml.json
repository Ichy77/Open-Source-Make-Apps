[
	// Defines "id" as mandatory module input parameter.
	{
		"name": "workflow_id",        // Makes value accesible via "{{parameters.id}}".
		"type": "text",  // Sets the type to unsigned integer.
		"label": "Workflow Id",  // Sets the user friendly label visible in the module.
		"required": true     // Sets the parameter as mandatory.
	},
	{
		"name": "run_id",        // Makes value accesible via "{{parameters.id}}".
		"type": "text",  // Sets the type to unsigned integer.
		"label": "Run Id",  // Sets the user friendly label visible in the module.
		"required": true     // Sets the parameter as mandatory.
	}
]