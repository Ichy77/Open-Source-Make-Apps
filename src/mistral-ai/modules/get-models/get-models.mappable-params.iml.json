// Defines "search" and "limit" as module input parameters.
[
	"rpc://getHtmlBanner",
	{
		"name": "limit",
		"type": "number",
		"label": "Limit",
		"default": 10,
		"help": "Maximum number of returned results (bundles)."
	}
]