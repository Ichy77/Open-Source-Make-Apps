{
    "url": "/sources/add-file",
    "body": {
        "file": {
            "value": "{{parameters.fileData}}",
            "options": {
                "filename": "{{parameters.fileName}}"
            }
        }
    },
    "type": "multipart/form-data",
    "method": "POST",
    "response": {
        "output": "{{body}}"
    }
}