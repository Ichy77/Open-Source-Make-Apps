{
	"url": "/v1/models",
	"method": "GET",
	"qs": {},
	"body": {},
	"headers": {},
	"response": {
		"iterate": "{{body.data}}",
		"output": {
			"label": "{{item.id}}",
			"value": "{{item.id}}"
		}
	}
}